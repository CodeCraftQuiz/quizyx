import { Link } from 'react-router-dom';

const Home = () => {
    return (
        <div className="min-h-[80vh] flex items-center justify-center">
            {/* Pusta strona - tylko tło i navbar */}
        </div>
    );
};

export default Home;
